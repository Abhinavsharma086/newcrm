-- Create database if not exists
CREATE DATABASE IF NOT EXISTS `new_erp` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Use the database
USE `new_erp`;

-- Grant privileges (if needed)
-- GRANT ALL PRIVILEGES ON new_erp.* TO 'root'@'localhost';
-- FLUSH PRIVILEGES;
